# Changesets

Hello and welcome! This folder contains Changeset metadata.

When you contribute changes that warrant a package release or changelog note, please add a changeset:

```bash
npm run changeset
```

Follow the prompts to choose your change level (`patch`, `minor`, `major`) and provide a description of the change. Commit the generated markdown file along with your code.
